# DOCX по корпоративному шаблону — ход работ

План: docs/superpowers/plans/2026-09-06-docx-corporate-template.md
Ветка: LocalTest
Старт: b933a2b (после коммита плана)

## Пре-флайт
- Task 9: тест-писатель без assert'ов противоречил рубрике ревью.
  Решение пользователя: сделать настоящим тестом. План поправлен.

## Задачи
Task 1: complete (commits 8e4aac4..0f92f9a, review clean)
  Ресурс 26 661 байт. Minor к финальному ревью:
  - в zip остался пустой directory-entry `word/` (артефакт JSZip, безвреден)
  - у скрипта нет пометки, где взять исходный .docx при перегенерации
Task 2: complete (commits 0f92f9a..a4bdaf5, review clean)
  Minor к финальному ревью:
  - UTF-8 BOM в ooxml.ts и __tests__/ooxml.test.ts, в style.ts его нет.
    Чинить одной волной в конце; в брифы дальше добавлено требование «без BOM».
Task 3: complete (commits a4bdaf5..65e87fd, review clean)
  Дефект плана, починен исполнителем: `__tests__/fixtures.ts` подхватывался
  дефолтным jest testMatch как пустой сьют. Ужат testMatch в jest.config.js.
  Проверено мной и ревьюером: все 22 тест-файла по-прежнему находятся.
  Minor к финальному ревью:
  - `indentFirstLine: 0` повторяется литералом; можно вынести IND_NONE в style.ts
Task 4: complete (commits 65e87fd..ab4c403, review clean после 1 фикса)
  НАСТОЯЩИЙ БАГ, найден и починен: в JS `\w` не покрывает кириллицу, поэтому
  `Ответственн\w*\s*:` никогда не совпадал с «Ответственный:». Метка не выносилась
  в отдельный жирный абзац ни в DOCX, ни в markdown. Починено в 4 местах
  lib/protocol-markdown-format.ts (27,33,149,161) + protocol-body.ts:229.
  Баг был перенесён в план из существующего кода — то есть жил в проде.
  Minor к финальному ревью:
  - 4 почти одинаковые копии паттерна `Срок|Ответственн[а-яё]*` — просится общий хелпер
  - protocol-body.ts вырос до 348 строк
  - biome/lint репозитория ругается на CRLF (325+ ошибок ДО наших изменений, pre-existing)

## Поправки плана по ходу
- Требование «npm run lint зелёный» снято: biome на базовом коммите 5d43ba0 уже даёт
  310 ошибок / 467 предупреждений по всему репозиторию. Новый критерий — 0 ошибок
  biome НА ФАЙЛАХ ЭТОЙ ВЕТКИ. Сейчас ветка добавила 18 своих (кавычки/формат) —
  чинить в финальной волне вместе с BOM.
Task 6: complete (commits 4a85831..29907e1, review clean)
  lib/docx-generator.ts: 421 строка -> 12 (обёртка над renderProtocolDocx).
  Зависимость `docx` убрана из package.json; осталась транзитивно через
  @mohtasham/md-to-docx, который снимает Task 8.
  Minor: npm install заодно нормализовал "surrealdb": "^1.3.2" -> "1.3.2" в lock.
Task 7: complete (commits 29907e1..7848d8e, review clean)
  ВТОРОЙ баг того же класса: в JS `\b` тоже определён через ASCII-`\w`, поэтому
  /^Договор\b/i и /\bот\s+/i никогда не совпадали с кириллицей — номер и дата
  договора молча терялись. Починено в parseContract.
  Круговой тест сравнивает buildProtocolBodyXml, все 4 фикстуры сходятся.
  Minor к финальному ревью:
  - новый циклический import type между protocol-markdown-format.ts и
    schemas/protocol-schema.ts (безвреден, стирается компилятором)
ОТКРЫТЫЙ ВОПРОС для финального ревью: три бага класса «JS-регулярки и кириллица»
  (\w в Task 4, \b в Task 7, копия в удалённом docx-generator.ts). Мои попытки
  просканировать репозиторий на остатки провалились из-за локали grep/python.
  Нужен направленный поиск в финальном ревью.
Task 8: complete (commits 7848d8e..e1952a6, review clean)
  Роут двухрежимный, клиентский md-to-docx удалён вместе с зависимостью.
  Контракт data-docx и обёртка деанонимизации не тронуты — проверено ревьюером:
  localDoc.content на клиенте уже деанонимизирован, утечки ПДн правка не создаёт.
  Minor к финальному ревью:
  - осиротел buildDocxMarkdown в lib/document/formatting.ts (вызывающих не осталось)
  - `buffer as BodyInit` в route.ts — артефакт типов @types/node, проверено, не маскирует дефект
  - biome по путям ветки: не 0 ошибок; почти всё — файлы прошлых задач + кавычки
Task 9: complete (commit 2694980, оснастка сверки)
  Шаг 6 (визуальная сверка) выполнен контроллером, НЕ субагентом.
  РЕЗУЛЬТАТ СВЕРКИ: пройдена по всем 10 пунктам. Логотип, колонтитул, Verdana,
  нумерация из numbering.xml, подчёркнутые заголовки, отсутствие серой заливки,
  «Согласовано» без рамок с асимметричными линиями подписи — всё совпадает
  с эталоном «Шаблон протокола с комм.docx».
  1 страница против 2 у эталона — ожидаемо (в эталоне сверху пояснения составителю).
  Minor: biome по путям ветки 30 ошибок (кавычки + файлы прошлых задач).
